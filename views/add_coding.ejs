<!-- <form action="/admin/exam/<%= examId %>/add/coding" method="POST">
    <label>Title:</label>
    <input type="text" name="questionTile" required><br>

    <label>Question Text:</label>
    <textarea name="questiontext" required></textarea><br>

    <label>Constraints:</label>
    <textarea name="constraits" required></textarea><br>

    <label>Input Format:</label>
    <textarea name="inputFormat" required></textarea><br>

    <label>Output Format:</label>
    <textarea name="outputFormat" required></textarea><br>

    <label>Sample Input:</label>
    <textarea name="sampleInput" required></textarea><br>

    <label>Sample Output:</label>
    <textarea name="sampleOutput" required></textarea><br>

    <label>Solution Template:</label>
    <textarea name="solutionTemplate" required></textarea><br>
    <label>Marks:</label>
    <input name="marks" required type="number"><br>


    <button type="submit">Add Coding Question</button>
</form> -->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Coding Question</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #141D2F; /* Dark blue background */
            font-family: Arial, sans-serif;
            color: #333;
            padding-bottom: 50px;
        }
        
        .container {
            margin-top: 50px;
            margin-bottom: 50px;
        }
        
        h1 {
            font-size: 2rem;
            margin-bottom: 25px;
            color: #ffffff;
            font-weight: 600;
            text-align: center;
        }
        
        /* Card styling */
        .card {
            border: 3px solid #1D4ED8;
            border-radius: 10px;
            box-shadow: 0 0 20px rgba(0, 123, 255, 0.2);
            background-color: #ffffff;
            margin-bottom: 25px;
            padding: 30px !important;
        }
        
        /* Form elements */
        .form-label {
            font-weight: 600;
            color: #0056b3;
            margin-bottom: 8px;
        }
        
        .form-control {
            border: 2px solid #b3d1ff;
            border-radius: 5px;
            padding: 10px 15px;
            margin-bottom: 20px;
            transition: all 0.3s ease;
        }
        
        .form-control:focus {
            border-color: #1D4ED8;
            box-shadow: 0 0 0 3px rgba(29, 78, 216, 0.25);
        }
        
        /* Textarea specific styling */
        textarea.form-control {
            min-height: 100px;
            resize: vertical;
            font-family: 'Consolas', 'Monaco', monospace;
            line-height: 1.5;
        }
        
        /* Code-related textareas */
        #solutionTemplate, #sampleInput, #sampleOutput {
            font-family: 'Consolas', 'Monaco', monospace;
            background-color: #f8f9fa;
        }
        
        /* Button styling */
        .btn-primary {
            background-color: #1D4ED8;
            border-color: #1D4ED8;
            padding: 10px 25px;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            background-color: #0056b3;
            border-color: #0056b3;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
        
        /* Back link */
        .back-link {
            margin-top: 20px;
            text-align: center;
        }
        
        .btn-link {
            color: #b3d1ff;
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s ease;
        }
        
        .btn-link:hover {
            color: #ffffff;
            text-decoration: underline;
        }
        
        /* Section dividers */
        .section-divider {
            margin: 30px 0;
            border-top: 1px solid #b3d1ff;
            position: relative;
        }
        
        .section-divider span {
            position: absolute;
            top: -12px;
            left: 50%;
            transform: translateX(-50%);
            background: white;
            padding: 0 15px;
            color: #0056b3;
            font-weight: 500;
        }
        
        /* Responsive adjustments */
        @media (max-width: 767px) {
            .container {
                padding: 15px;
                margin-top: 30px;
            }
            
            .card {
                padding: 20px !important;
            }
            
            h1 {
                font-size: 1.8rem;
            }
            
            textarea.form-control {
                min-height: 80px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Add Coding Question</h1>

        <!-- Add Coding Question Form -->
        <div class="card">
            <form action="/admin/exam/<%= examId %>/add/coding" method="POST">
                <!-- Basic Information -->
                <div class="mb-4">
                    <label for="questionTile" class="form-label">Title:</label>
                    <input type="text" id="questionTile" name="questionTile" class="form-control" 
                           placeholder="Enter a descriptive title for the question" required>
                </div>

                <div class="mb-4">
                    <label for="questiontext" class="form-label">Question Text:</label>
                    <textarea id="questiontext" name="questiontext" class="form-control" 
                              placeholder="Describe the problem in detail" required></textarea>
                </div>

                <div class="section-divider">
                    <span>Specifications</span>
                </div>

                <!-- Specifications -->
                <div class="mb-4">
                    <label for="constraints" class="form-label">Constraints:</label>
                    <textarea id="constraints" name="constraits" class="form-control" 
                              placeholder="List all constraints (e.g., 1 ≤ n ≤ 10^5)" required></textarea>
                </div>

                <div class="mb-4">
                    <label for="inputFormat" class="form-label">Input Format:</label>
                    <textarea id="inputFormat" name="inputFormat" class="form-control" 
                              placeholder="Describe how the input will be formatted" required></textarea>
                </div>

                <div class="mb-4">
                    <label for="outputFormat" class="form-label">Output Format:</label>
                    <textarea id="outputFormat" name="outputFormat" class="form-control" 
                              placeholder="Describe how the output should be formatted" required></textarea>
                </div>

                <div class="section-divider">
                    <span>Examples</span>
                </div>

                <!-- Examples -->
                <div class="mb-4">
                    <label for="sampleInput" class="form-label">Sample Input:</label>
                    <textarea id="sampleInput" name="sampleInput" class="form-control" 
                              placeholder="Provide sample input" required></textarea>
                </div>

                <div class="mb-4">
                    <label for="sampleOutput" class="form-label">Sample Output:</label>
                    <textarea id="sampleOutput" name="sampleOutput" class="form-control" 
                              placeholder="Provide expected output for the sample input" required></textarea>
                </div>

                <div class="section-divider">
                    <span>Code</span>
                </div>

                <!-- Code and Evaluation -->
                <div class="mb-4">
                    <label for="solutionTemplate" class="form-label">Solution Template:</label>
                    <textarea id="solutionTemplate" name="solutionTemplate" class="form-control" rows="6"
                              placeholder="Provide starter code for students (e.g., function signature)" required></textarea>
                </div>

                <div class="mb-4">
                    <label for="marks" class="form-label">Marks:</label>
                    <input type="number" id="marks" name="marks" class="form-control" min="1" value="10" required>
                </div>

                <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                    <button type="submit" class="btn btn-primary">Add Coding Question</button>
                </div>
            </form>
        </div>

        <!-- Back Link -->
        <div class="back-link">
            <a href="/admin/exam/questions/<%= examId %>" class="btn btn-link">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-arrow-left me-2" viewBox="0 0 16 16">
                    <path fill-rule="evenodd" d="M15 8a.5.5 0 0 0-.5-.5H2.707l3.147-3.146a.5.5 0 1 0-.708-.708l-4 4a.5.5 0 0 0 0 .708l4 4a.5.5 0 0 0 .708-.708L2.707 8.5H14.5A.5.5 0 0 0 15 8z"/>
                </svg>
                Back to Questions
            </a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
