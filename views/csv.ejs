<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MCQ Question Uploader</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>MCQ Question Management</h1>
        
        <div class="upload-section">
            <button id="showUploadFormBtn" class="btn btn-primary">Upload MCQ Questions</button>
            
            <div id="uploadFormContainer" style="display:none; margin-top: 20px;">
              <form action="/admin/exam/<%= examId %>/upload-mcq-csv" method="POST" enctype="multipart/form-data">
                <div class="form-group">
                  <label for="csvFile">Select CSV file containing MCQ questions:</label>
                  <input type="file" name="csvFile" id="csvFile" accept=".csv" required class="form-control">
                </div>
                <div class="form-group">
                  <label for="createdBy">Created By:</label>
                  <input type="text" name="createdBy" id="createdBy" required class="form-control">
                </div>
                <button type="submit" class="btn btn-success">Upload</button>
              </form>
              
              <div class="mt-3">
                <p><strong>CSV Format Requirements:</strong></p>
                <p>Your CSV file should have the following columns:</p>
                <ul>
                  <li>classification - Category of the question</li>
                  <li>question - The MCQ question text</li>
                  <li>option1, option2, option3, option4 - The four answer options</li>
                  <li>correctAnswer - The correct answer (must match one of the options)</li>
                  <li>level - Difficulty level (easy, medium, or hard)</li>
                  <li>marks - Number of points for the question</li>
                </ul>
              </div>
            </div>
        </div>
    </div>
      
    <script>
        document.getElementById('showUploadFormBtn').addEventListener('click', function() {
          const container = document.getElementById('uploadFormContainer');
          if (container.style.display === 'none') {
            container.style.display = 'block';
          } else {
            container.style.display = 'none';
          }
        });
    </script>
</body>
</html>